# Configuracion del servidor

1. Para iniciar todos los taps en el servidor se debe ejecutar el script *start-server.sh* de la siguiente forma:

   $ sudo ./start-server.sh

2. Para parar todos los cliente p2p de openvpn ejecutar *stop-server.sh* de la siguiente forma:

   $ sudo ./stop-server.sh